# Avessa

Never a Stranger — social travel MVP built with Next.js and Supabase.

## Local setup

1. Copy `.env.example` to `.env.local` and set your Supabase URL + publishable key.
2. `npm install`
3. `npm run dev`
