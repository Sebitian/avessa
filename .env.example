# From Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://masbhjkrknhukfsvxxzj.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Optional: Google Maps Platform (Places + Geocoding) — server-only
# Enables live nearby venues, location autocomplete, and reverse geocode on Explore.
# Falls back to Nominatim + curated mock places if unset.
GOOGLE_MAPS_API_KEY=
